#include<iostream>

using namespace std;

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int A, B, V;
	cin >> A >> B >> V;

	cout << (V - B + A - B - 1) / (A - B);

	//반올림(A/B)
	//===> (A+B-1)/B -> 반올림 하는거!!

	return 0;
}


